{
  "name": "stepsolver-web-vendor",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:pages": "node build-pages.mjs",
    "test": "node --test editor-contract.test.mjs"
  },
  "dependencies": {
    "@cortex-js/compute-engine": "0.58.0",
    "mathlive": "0.110.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
